# Backend GPU API URL (for frontend to call)
# Copy this file to .env.local and adjust the URL:
#   cp .env.example .env.local
#
# Local development (backend on same machine):
NEXT_PUBLIC_API_URL=http://localhost:8000
#
# Production (GPU server):
# NEXT_PUBLIC_API_URL=https://api.splattie.ai
